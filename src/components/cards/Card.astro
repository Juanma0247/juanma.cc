---
export interface Props {
  href: string
  title: string
  image: string
}

const { href, title, image } = Astro.props
---

<a href={href} class="card">
  <h2>{title}</h2>
  <img class="card-img" src={`/img/cards/${image}`} alt={title} />
</a>
