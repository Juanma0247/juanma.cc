---
import Card from './Card.astro'

export interface Props {
  slug: string
  title: string
  image: string
}

const { slug, title, image } = Astro.props
---

<Card href={`/games/${slug}`} title={title} image={image} />
